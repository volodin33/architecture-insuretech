apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  namespace: default
  labels:
    app: scaletestapp
spec:
  selector:
    app: scaletestapp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
